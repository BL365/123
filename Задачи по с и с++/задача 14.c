//что выведется на экран(решать без запуска), почему. в чем ошибка?

/*
задача ни понимание типов данных, и формат вывода.
*/
#include <cstdlib>  
#include <stdio.h> 

int main() 
{ 
	unsigned int i;
	for (i = 100; i >= 0; --i)
		printf("%d\n", i); 
}
